async function archiveNotionEntry(pageId) {
    // Moves page to Archive Database[span_8](start_span)[span_8](end_span)
    console.log(`🗄️ [NOTION] Archiving entry: ${pageId}`);
}
module.exports = { archiveNotionEntry };
